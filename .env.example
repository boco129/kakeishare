# Database
# 必須: "file:" で始まること（例: file:./prisma/dev.db）
DATABASE_URL="file:./prisma/dev.db"

# Auth.js v5
# 必須: 最低32文字のランダム文字列（生成: openssl rand -base64 32）
# プレースホルダー文字列は起動時に拒否されます
AUTH_SECRET="replace-with-long-random-secret"
# 必須: 有効なURL形式
AUTH_URL="http://localhost:3000"

# Claude AI連携（Phase 4）
# 任意: 未設定でもPhase 3機能は正常動作します
# 取得: https://console.anthropic.com/
# ANTHROPIC_API_KEY="sk-ant-..."
